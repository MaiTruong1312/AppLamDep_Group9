import 'package:flutter/material.dart';

class GiftcardTab extends StatelessWidget {
  const GiftcardTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Giftcard is coming soon"));
  }
}